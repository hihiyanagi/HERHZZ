# FastAPI框架和相关依赖
fastapi==0.104.1
uvicorn[standard]==0.24.0

# JWT Token处理
PyJWT==2.8.0

# 环境变量处理
python-dotenv==1.0.0

# HTTP客户端（如果需要调用其他API）
httpx==0.25.2

# 数据验证（Pydantic已包含在FastAPI中）
pydantic==2.5.0 